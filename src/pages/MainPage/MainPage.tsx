import React from 'react';

import Header from '../../components/Header/Header';

const MainPage: React.FC = () => {
  return (
    <div>
      <Header activePage={1}></Header>
    </div>
  );
};

export default MainPage;
